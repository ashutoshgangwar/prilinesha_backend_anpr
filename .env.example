# Server port
PORT=5000

# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/vehicle_lpr

# Shared secret that the Intozi camera server must send in the "x-api-key" header
INTOZI_API_KEY=change-me-to-a-long-random-secret
